name: Build
on: push

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: julia --project=. triangles.jl
      - uses: actions/upload-artifact@v4
        with:
          path: out.svg
