name: Build
on: push

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: cargo run --release
      - uses: actions/upload-artifact@v4
        with:
          path: out.svg
